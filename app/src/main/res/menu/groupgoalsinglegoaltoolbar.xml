<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

        <!-- "Mark Favorite", should appear as action button if possible -->
        <item
            android:id="@+id/groupMemberBtn"
            android:icon="@drawable/groupmembers_foreground"
            android:title="action_setting"
            app:showAsAction="ifRoom"/>

        <item
            android:id="@+id/inviteUserBtn"
            android:icon="@drawable/inviteuser_foreground"
            android:title="action_setting"
            app:showAsAction="ifRoom"/>

        <item
            android:id="@+id/adminPlusBtn"
            android:icon="@drawable/adminplus_foreground"
            android:title="action_setting"
            app:showAsAction="ifRoom"/>


</menu>