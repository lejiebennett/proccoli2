package com.example.proccoli2.NewModels;

import java.util.ArrayList;


public class MainProgressViewDataModel {
    /*
    ArrayList<ArrayList<singleDataPointForBarChart>> activeChartData = new ArrayList<>();
    ArrayList<ArrayList<singleDataPointForBarChart>> expiredChartData = new ArrayList<>();
    ArrayList<ArrayList<singleDataPointForBarChart>> finishedChartData = new ArrayList<>();

    double chartScale = 10.0;

    public MainProgressViewDataModel(){
        if() let activeData =	TabbarVC.sharedInstance?.profileVCInstance?.activePersonal){
            dataPrep(data: activeData, dataType: SEGMENTED_CONTROL_ACTIVE_REF);
        }
        if let finishedData = TabbarVC.sharedInstance?.profileVCInstance?.finishedPersonal {
            dataPrep(data: finishedData, dataType: SEGMENTED_CONTROL_FINISHED_REF);
        }
        if let expiredData = TabbarVC.sharedInstance?.profileVCInstance?.expiredPersonal {
            dataPrep(data: expiredData, dataType: SEGMENTED_CONTROL_EXPIRED_REF);
        }
    }

     */



}
